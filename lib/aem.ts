/**
 * AEM Content Fragment API Client
 * Handles fetching content fragments from Adobe Experience Manager\n * Synchronized with Alex's VolvoHomepage Content Fragment Model
 * 
 * CRITICAL: All environment variable access is moved INTO function scope,
 * NOT at module load time. This prevents build failures when env vars are undefined.
 */\n\n/**\n * Type definitions for expected AEM Content Fragment fields\n */\nexport interface HeroContent {\n  headline: string;\n  subheadline: string;\n  ctaText: string;\n  ctaUrl: string;\n  backgroundImageUrl: string;\n}\n\nexport interface NavLink {\n  label: string;\n  url: string;\n}\n\nexport interface Navigation {\n  logo: string;\n  links: NavLink[];\n}\n\nexport interface CarModel {\n  name: string;\n  tagline: string;\n  imageUrl: string;\n  ctaLink: string;\n}\n\nexport interface Feature {\n  title: string;\n  description: string;\n  iconUrl: string;\n}\n\nexport interface FeatureSection {\n  headline: string;\n  features: Feature[];\n}\n\nexport interface FooterLink {\n  label: string;\n  url: string;\n}\n\nexport interface Footer {\n  copyrightText: string;\n  links: FooterLink[];\n}\n\nexport interface VolvoHomepageContent {\n  hero: HeroContent;\n  navigation: Navigation;\n  carModels: CarModel[];\n  features: FeatureSection;\n  footer: Footer;\n}\n\n/**\n * Get AEM endpoint URL from environment variables.\n * Checks multiple env var names for compatibility with different deploy platforms.\n * IMPORTANT: Called INSIDE functions, not at module scope.\n */\nfunction getAEMEndpoint(): string {\n  return (\n    process.env.AEM_PUBLISH_ENDPOINT ||\n    process.env.AEM_GRAPHQL_ENDPOINT ||\n    process.env.AEM_ENDPOINT ||\n    ''\n  );\n}\n\n/**\n * Get AEM authentication token from environment.\n * IMPORTANT: Called INSIDE functions, not at module scope.\n */\nfunction getAEMToken(): string {\n  return process.env.AEM_TOKEN || '';\n}\n\n/**\n * Fetch content fragment from AEM using GraphQL\n * @param fragmentPath - Path to the content fragment in AEM DAM\n * @returns Promise with the content fragment data, or null if fetch fails\n * \n * IMPORTANT: This function returns null on error rather than throwing.\n * This allows server components to gracefully fall back to default content\n * when AEM is unavailable (e.g., during Vercel build).\n */\nexport async function getContentFragment(fragmentPath: string): Promise<any> {\n  const endpoint = getAEMEndpoint();\n  const token = getAEMToken();\n\n  if (!endpoint) {\n    console.warn('AEM_ENDPOINT, AEM_PUBLISH_ENDPOINT, or AEM_GRAPHQL_ENDPOINT not configured');\n    return null;\n  }\n\n  const query = `\n    query {\n      contentFragmentByPath(_path: \"${fragmentPath}\") {\n        item {\n          __typename\n          _path\n          ... on VolvoHomepage {\n            heroHeadline: headline\n            heroSubheadline: subheadline\n            heroCtaText: ctaText\n            heroCtaUrl: ctaUrl\n            heroBackgroundImage: backgroundImageUrl\n            navLogo: logo\n            navLinks: navigationLinks {\n              linkLabel: label\n              linkUrl: url\n            }\n            carModelsList: models {\n              modelName: name\n              modelTagline: tagline\n              modelImage: imageUrl\n              modelCta: ctaLink\n            }\n            featureHeadline: headline\n            featuresList: featureItems {\n              featureTitle: title\n              featureDesc: description\n              featureIcon: iconUrl\n            }\n            footerCopyright: copyrightText\n            footerLinksList: footerLinks {\n              linkLabel: label\n              linkUrl: url\n            }\n          }\n        }\n      }\n    }\n  `;\n\n  const headers: HeadersInit = {\n    'Content-Type': 'application/json',\n  };\n\n  if (token) {\n    headers['Authorization'] = `Bearer ${token}`;\n  }\n\n  try {\n    const response = await fetch(endpoint, {\n      method: 'POST',\n      headers,\n      body: JSON.stringify({ query }),\n      cache: 'no-store',\n    });\n\n    if (!response.ok) {\n      console.error(`AEM API returned ${response.status}: ${response.statusText}`);\n      return null;\n    }\n\n    const data = await response.json();\n\n    if (data.errors) {\n      console.error('GraphQL Errors:', data.errors);\n      return null;\n    }\n\n    return data.data?.contentFragmentByPath?.item || null;\n  } catch (error) {\n    console.error('Failed to fetch content fragment:', error);\n    return null;\n  }\n}\n\n/**\n * Fetch multiple content fragments with filtering\n * @param fragmentType - Type of content fragment to fetch\n * @returns Promise with array of content fragments, or empty array if fetch fails\n * \n * IMPORTANT: This function returns empty array on error rather than throwing.\n */\nexport async function getContentFragments(fragmentType: string): Promise<any[]> {\n  const endpoint = getAEMEndpoint();\n  const token = getAEMToken();\n\n  if (!endpoint) {\n    console.warn('AEM_ENDPOINT, AEM_PUBLISH_ENDPOINT, or AEM_GRAPHQL_ENDPOINT not configured');\n    return [];\n  }\n\n  const query = `\n    query {\n      contentFragmentList(limit: 100) {\n        items {\n          __typename\n          _path\n        }\n      }\n    }\n  `;\n\n  const headers: HeadersInit = {\n    'Content-Type': 'application/json',\n  };\n\n  if (token) {\n    headers['Authorization'] = `Bearer ${token}`;\n  }\n\n  try {\n    const response = await fetch(endpoint, {\n      method: 'POST',\n      headers,\n      body: JSON.stringify({ query }),\n      cache: 'no-store',\n    });\n\n    if (!response.ok) {\n      console.error(`AEM API returned ${response.status}: ${response.statusText}`);\n      return [];\n    }\n\n    const data = await response.json();\n\n    if (data.errors) {\n      console.error('GraphQL Errors:', data.errors);\n      return [];\n    }\n\n    return data.data?.contentFragmentList?.items || [];\n  } catch (error) {\n    console.error('Failed to fetch content fragments:', error);\n    return [];\n  }\n}\n\n/**\n * Transform raw AEM response to typed content structure\n * Maps GraphQL field aliases to component props\n */\nexport function transformAEMContent(aemData: any): VolvoHomepageContent {\n  return {\n    hero: {\n      headline: aemData.heroHeadline || 'Welcome to Volvo',\n      subheadline: aemData.heroSubheadline || 'Luxury and safety combined',\n      ctaText: aemData.heroCtaText || 'Explore Models',\n      ctaUrl: aemData.heroCtaUrl || '/models',\n      backgroundImageUrl: aemData.heroBackgroundImage || '/images/hero-bg.jpg',\n    },\n    navigation: {\n      logo: aemData.navLogo || '',\n      links: (aemData.navLinks || []).map((link: any) => ({\n        label: link.linkLabel || link.label || '',\n        url: link.linkUrl || link.url || '',\n      })),\n    },\n    carModels: (aemData.carModelsList || []).map((model: any) => ({\n      name: model.modelName || model.name || '',\n      tagline: model.modelTagline || model.tagline || '',\n      imageUrl: model.modelImage || model.imageUrl || '',\n      ctaLink: model.modelCta || model.ctaLink || '',\n    })),\n    features: {\n      headline: aemData.featureHeadline || 'Why Choose Volvo',\n      features: (aemData.featuresList || []).map((feature: any) => ({\n        title: feature.featureTitle || feature.title || '',\n        description: feature.featureDesc || feature.description || '',\n        iconUrl: feature.featureIcon || feature.iconUrl || '',\n      })),\n    },\n    footer: {\n      copyrightText: aemData.footerCopyright || '© 2024 Volvo Cars',\n      links: (aemData.footerLinksList || []).map((link: any) => ({\n        label: link.linkLabel || link.label || '',\n        url: link.linkUrl || link.url || '',\n      })),\n    },\n  };\n}\n